# Architecture

This document describes the fixture project's own structure. Like the
retros and planning docs, it follows the codecompass practice for
keeping dependency context current — see the `codecompass` Skill.
