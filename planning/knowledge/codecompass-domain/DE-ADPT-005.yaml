id: DE-ADPT-005
kind: derivation
claim: CL-ADPT-005
method: >
  Read core.py's own two dataclass/enum definitions directly rather than
  inferring the relationship from usage alone, then read decisions/0002
  to confirm the *reason* an adapter exists as a separate concept from a
  vendor record (per-ecosystem tooling logic that a plain config field
  couldn't express). Deliberately looked for a concrete counterexample
  to "one vendor maps cleanly to one adapter instance operating over its
  own directory" rather than stopping at the clean textbook definition
  -- re-read HaskellAdapter's monorepo-resolution code (already read for
  DE-ADPT-002) specifically through this lens and confirmed it is a real,
  evidenced edge case (a documented, deliberate design point --
  decisions/0057's own "Monorepo package roots" section -- not a bug),
  worth stating explicitly on the concept page rather than presenting
  the three-way distinction as cleaner than it actually is in every
  case.
inputs: [EV-ADPT-005, EV-ADPT-002]
performed_by: "context-researcher agent dispatch"
timestamp: "2026-09-23T00:00:00Z"
