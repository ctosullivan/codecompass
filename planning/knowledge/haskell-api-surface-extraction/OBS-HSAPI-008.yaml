id: OBS-HSAPI-008
kind: observation
method: source_read
what_was_done: >
  Read hledger/hledger.cabal's `library` stanza (lines 104-190),
  specifically its `exposed-modules` list (lines 105-144, 40 modules
  under Hledger.Cli.*) and `other-modules` (line 146, only
  `Paths_hledger`), for comparison against hledger-lib's own
  exposed/other split (OBS-HSAPI-007) -- checking whether the
  package-level visibility boundary is a hledger-lib-only quirk or a
  repeated pattern across this codebase's packages.
tool: null
tool_version: null
repository_revision: "33fa849e7ae841968bd21c427094c4fb4a4ec38d"
location: "hledger/hledger.cabal:104-190"
raw_result: >
  Same shape as hledger-lib: every real Hledger.Cli.* source module
  (Accounts, Activity, Add, ... Version, plus Hledger.Cli itself) is
  listed under exposed-modules; only the autogenerated Paths_hledger is
  under other-modules. Confirms this is a repeated pattern, not a
  hledger-lib-specific accident: in this codebase, package authors
  expose nearly every module they ship, so the exposed-modules/
  other-modules boundary rarely excludes anything a per-file scanner
  would otherwise process.
timestamp: "2026-09-19T00:00:00Z"
performed_by: "context-researcher agent dispatch"
status: recorded
