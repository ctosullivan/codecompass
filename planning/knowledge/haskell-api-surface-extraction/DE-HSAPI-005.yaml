id: DE-HSAPI-005
kind: derivation
claim: CL-HSAPI-005
method: >
  Per the task's own explicit instruction to check `package.yaml`/
  `.cabal` for `exposed-modules` vs `other-modules`, read both directly
  for hledger-lib, then computed the real diff between "every .hs file
  physically present in the source tree" and "the exposed-modules list"
  to see whether the boundary would actually change a naive scanner's
  behaviour, rather than assuming its mere existence is sufficient
  evidence it matters. The diff was almost empty (only Setup.hs and
  autogenerated Paths_/PackageInfo_ modules). Explicitly checked the
  sibling hledger package too, per this role's own "trace every real
  consumer, don't stop at the first" discipline, to see if hledger-lib
  was an unusual, unrepresentative case -- it was not; the same
  near-total-exposure pattern repeated. This directly parallels this
  project's own prior "Gap 1/Gap 2" precedent (packet-sufficiency.md,
  cited in this role's own brief): a real mechanism can be correctly
  identified and still be effectively invisible to a test run against
  one specific reference project's own accidental structure, and that
  should be named honestly as a testing-coverage gap rather than quietly
  treated as "confirmed unimportant."
inputs:
  - EV-HSAPI-006
performed_by: "context-researcher agent dispatch"
timestamp: "2026-09-19T00:00:00Z"
