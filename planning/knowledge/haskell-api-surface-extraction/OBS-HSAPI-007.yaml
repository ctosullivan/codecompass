id: OBS-HSAPI-007
kind: observation
method: source_read
what_was_done: >
  Read hledger-lib/hledger-lib.cabal's `exposed-modules`/`other-modules`
  lists (lines 54-125) and hledger-lib/package.yaml's own comment about
  the default (line 122, "default: All modules in source-dirs less
  other-modules less modules mentioned in when"). Then enumerated every
  real `.hs` file physically present under hledger-lib/ (excluding
  test/ and build directories) and diffed that set against the
  `exposed-modules` list.
tool: bash (find/sed/comm)
tool_version: null
repository_revision: "33fa849e7ae841968bd21c427094c4fb4a4ec38d"
location: "hledger-lib/hledger-lib.cabal:54-125, hledger-lib/package.yaml:122"
raw_result: |
  exposed-modules count: 65
  real .hs modules found in source tree (excl. test/, build dirs): 68
  In source tree but NOT in exposed-modules:
    Setup
    .stack-work.dist.aarch64-linux-tinfo6.ghc-9.12.4.build.autogen.PackageInfo_hledger_lib
    .stack-work.dist.aarch64-linux-tinfo6.ghc-9.12.4.build.autogen.Paths_hledger_lib
  i.e. every genuine hand-written hledger-lib source module is exposed;
  the only non-exposed entries found are the package's own Setup.hs and
  two GHC/Cabal-autogenerated modules (Paths_/PackageInfo_) picked up
  incidentally by the find command from .stack-work build output, not
  real hand-authored source.
timestamp: "2026-09-19T00:00:00Z"
performed_by: "context-researcher agent dispatch"
status: recorded
